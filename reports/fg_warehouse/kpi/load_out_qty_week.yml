query: select week_shipped, year_shipped, container_type_plts, flatbed_type_plts, container_type_cartons, flatbed_type_cartons, container_type_count, flatbed_type_count,  'row ' || CAST(ROW_NUMBER() OVER() AS character varying) AS id  from vw_load_out_qty_week where (week_shipped={week_shipped} and year_shipped={year_shipped})
main_table_name: load_vehicles
fields:
