query: select id, presort_child_run_id, bin1, bin2, bin3, bin1_error, bin2_error, bin3_error, created_at FROM bin_staging_errors where (created_at={created_at})
main_table_name: bin_staging_errors
fields:
 field1:
  field_name: created_at
  caption: created_at
  field_type: daterange